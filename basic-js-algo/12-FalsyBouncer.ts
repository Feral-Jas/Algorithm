//* Description: bounce falsy values in an array and return the bounced array
//? e.g. [null,false,0,"",undefined] should return []
function bouncer(arr: Array<any>) {
    //hint try convert each value to boolean
    arr.forEach((item) => {
        //check boolean
    });
}
